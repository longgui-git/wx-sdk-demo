<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.trawe.operation.base.third.dbaccess.mapper.WeixinAccountConfigMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.trawe.operation.base.third.dbaccess.domain.WeixinAccountConfig">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="appid" property="appid" />
        <result column="alias" property="alias" />
        <result column="app_secret" property="appSecret" />
        <result column="token" property="token" />
        <result column="aes_key" property="aesKey" />
        <result column="is_encrypt" property="isEncrypt" />
        <result column="notify_url" property="notifyUrl" />
        <result column="state" property="state" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, appid, alias, app_secret, token, aes_key, is_encrypt, notify_url, state, create_time, update_time
    </sql>

</mapper>
